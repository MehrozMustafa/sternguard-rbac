export const ROUTES = {
  USER: {
    REGISTER: "/register",
    LOGIN: "/login",
    ME: "/me",
    LIST: "/users",      // List all users
    CREATE: "/users",    // Create a new user (POST)
    DELETE: "/users/:id" // Delete a user by ID
  },
};
